# 隐私与数据处理说明

## 本机保存的数据

程序在 Windows 当前用户的应用数据目录中保存：

- 材料原始路径或网页地址
- 文件哈希、文件名、大小和修改时间
- 已提取的结构化字段
- 最多 4,000 个字符的正文片段，用于识别结果检查
- 客户分组、客户别名、优先级、缺失字段和敏感信息类型
- 应用设置

原始文档继续保存在原位置，程序不会移动、重命名或覆盖它们。

## 网络行为

程序只有以下明确的网络行为：

1. 用户主动输入网页链接后，程序从该链接下载 HTML 正文，限制为 HTTP/HTTPS 和 10MB。
2. Google Drive 或 SharePoint 文件由用户安装的官方同步客户端下载；程序只读取同步后的本地文件。
3. 启用本地模型时，程序只连接 `localhost`、`127.0.0.1` 或 `::1` 上的 Ollama 接口。

程序不包含远程 AI API，也不允许把本地模型端点配置为互联网地址。

## 不可信文档内容

所有导入材料及网页都被视为不可信数据：

- 网页脚本不会执行。
- 文档内出现的“忽略要求”“运行命令”“上传文件”等文字只作为被分析的正文。
- 本地模型只负责返回固定的结构化字段。
- 模型返回的未知键和值会被丢弃，不能触发系统命令、文件操作或网络操作。

## 敏感信息

程序可检测身份证号、手机号和车牌号，并在索引和报表中标记材料包含敏感信息。由于数据库本身包含客户字段，建议：

- 使用 Windows 登录密码和磁盘加密；
- 不把应用数据目录加入公共云同步；
- 导出的 Excel 报表只交给获得授权的人员；
- 对不再需要的报表按单位的数据保留政策处理。

## 公开源代码与客户数据

数据库、日志和本地环境文件已通过 `.gitignore` 排除。提交到公开 GitHub 仓库前仍应检查变更，确保没有测试用真实客户信息、截图、报表或数据库文件。
